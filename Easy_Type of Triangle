SELECT CASE
WHEN A=B AND B=C THEN 'Equilateral'
WHEN ((A=B AND B<>C) OR (A=C AND B<>C)) AND A+B>C THEN 'Isosceles'
WHEN (A<>B AND B<>C AND C<>A) AND A+B>C THEN 'Scalene'
ELSE 'Not A Triangle'
END
FROM TRIANGLES;
